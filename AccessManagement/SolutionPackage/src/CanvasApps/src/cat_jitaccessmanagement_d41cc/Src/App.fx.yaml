App As appinfo:
    BackEnabled: =false
    OnStart: =If(Not(IsBlank(Param("environment"))),Set(environmentFilter,Param("environment")),Set(environmentFilter, Blank()));

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

